# First experiment — tiny_cnn on FER2013 with the baseline train config.
# The pipeline's "does the whole thing actually run?" reference.

dataset:  fer2013
model:    tiny_cnn
config:   baseline
seed:     42

phases:
  - setup
  - prepare_dataset
  - train
