how the bleep am I going to get myself on track here

1) read through the chat bot and model python code
2) read through all the tickets to get context of what can be moved forward
3) look at example presentation to get an idea of what is needed
4) use that as direction to look for other resources
5) chose THREE models to compare to 
    - look up the original data set
    - get the dataset dynamically downloadable into the repo
    - identify paper models that have trained on the data set
6) more roaming plan bullet points to be added